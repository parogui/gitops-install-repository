# Apply after OpenShift GitOps (or another Argo CD with ApplicationSet) is running.
# See README.md for bootstrap order.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace-argocd.yaml
  - appproject.yaml
  - applicationset.yaml
